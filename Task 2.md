
### Task 2 — Baseline (10 pts)

A single LLM call with no tools. Returns `AgentResult` with `tools_called = []`.
This is your control condition. If your agents don't outperform it, the added complexity isn't justified.


